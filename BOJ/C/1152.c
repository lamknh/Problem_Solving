#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>

int main(void) {
	//문자 앞 뒤 공백 있을 수 있음, 단어 수 세기
	char sentence[100];
	int word = 0;

	scanf("%s", sentence);

	while (1) {
		char c = getchar();
		if (c == ' ') {

		}
	}
	return 0;
}